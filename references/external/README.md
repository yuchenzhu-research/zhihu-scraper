# External References

这个目录用于存放**本地挂载的外部参考仓库**。

当前典型内容包括：

- `MediaCrawler/`
- `openclaw/`

这些目录的定位是：

- 供人工阅读和架构参考
- 不属于主项目正式代码边界
- 不作为运行时依赖
- 不默认进入版本管理

使用原则：

- 可以参考它们的结构和实现思路
- 不应让主项目代码直接 import 这里的模块
- 如果某个外部参考结论需要长期保留，应重新写入 `docs/` 或 `MANUAL.md`
